build:
  image: node:4.4.7
  commands:
    - apt-get -yqq update
    - apt-get -yqq install libelf-dev
    - npm install jspm@beta
    - npm install
    - node_modules/.bin/jspm install
    - npm test
cache:
  mount:
    - jspm_packages
    - node_modules
